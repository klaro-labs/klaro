# Klaro contracts — environment template.
# Copy to .env and fill before running `forge script`.

ARC_TESTNET_RPC_URL="https://rpc.testnet.arc.network"
PRIVATE_KEY=""          # deployer EOA — testnet ONLY, NEVER mainnet keys here
ARCSCAN_API_KEY=""      # for `forge verify-contract` on testnet.arcscan.app
